name: auth_repository
description: A repository that handles authentication requests.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  supabase_flutter: ^2.3.4
  equatable: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
